# دليل تشغيل ونشر تطبيق "عبد سيف" 🚀

هذا الدليل يشرح **خطوة بخطوة** كيف تطلع تطبيقك على ثلاثة أشكال:

1. 🌐 **موقع ويب** — يفتح من أي متصفح بأي جهاز (مجاني، تلقائي).
2. 📱 **APK لأندرويد** — تطبيق رسمي تنصبه على الموبايل (يبنى تلقائياً).
3. 💻 **EXE لويندوز** — تطبيق رسمي تنصبه على اللابتوب (يبنى تلقائياً).
4. ☁️ **مزامنة تلقائية** بين كل الأجهزة عبر Supabase (مجانية).

---

## 🟢 المرحلة 1: نشر التطبيق على الإنترنت (10 دقائق)

### 1.1 تفعيل GitHub Pages

1. ادخل على المستودع في GitHub.
2. **Settings** → **Pages** (من القائمة الجانبية اليسرى).
3. تحت "Build and deployment" → **Source** اختر `GitHub Actions`.
4. احفظ.

> ✅ بعد أول push، سيشتغل workflow اسمه **Deploy to GitHub Pages** تلقائياً ويعطيك رابط بالشكل:
> ```
> https://sa12211221if-bot.github.io/-/
> ```

### 1.2 افتح الرابط من جوالك ولابتوبك للاختبار
لازم تشوف التطبيق يفتح طبيعي. ✅

---

## ☁️ المرحلة 2: إعداد المزامنة السحابية (Supabase) — مرة وحدة فقط

هذه الخطوة تخلي بياناتك تتزامن **تلقائياً** بين كل الأجهزة (موبايل + لابتوب + ويب).

### 2.1 إنشاء مشروع Supabase (مجاني)

1. روح على [supabase.com](https://supabase.com) واضغط **Start your project**.
2. سجل دخول بحساب GitHub.
3. اضغط **New Project**:
   - **Name**: `abd-saif`
   - **Database password**: اختر باسوورد قوي واحفظه (بمكان آمن)
   - **Region**: اختر الأقرب لك (مثلاً `Frankfurt (eu-central-1)`)
4. اضغط **Create new project** وانتظر دقيقتين.

### 2.2 تنفيذ سكربت قاعدة البيانات

1. في لوحة Supabase: **SQL Editor** (أيقونة قاعدة البيانات بالجانب).
2. اضغط **New query**.
3. افتح ملف [`supabase/schema.sql`](./supabase/schema.sql) من المشروع، انسخ كل محتواه والصقه.
4. اضغط **Run** (أو Ctrl+Enter).

> ✅ هذا ينشئ جدول `app_data` مع نظام أمان صارم (كل مستخدم يشوف بياناته فقط).

### 2.3 الحصول على بيانات الاتصال

1. **Project Settings** (أيقونة الترس) → **API**.
2. انسخ:
   - **Project URL** (مثل `https://xxxx.supabase.co`)
   - **anon public** key (المفتاح الطويل تحت "Project API keys")

### 2.4 ربط التطبيق بحسابك

1. افتح التطبيق (من الرابط في المرحلة 1).
2. روح على **الإعدادات**.
3. في قسم **الحساب والمزامنة** → **إعداد المزامنة**.
4. الصق `Project URL` و `anon public key` → **حفظ**.
5. اضغط **إنشاء حساب** → أدخل بريدك وكلمة سر → **Sign up**.
6. (إذا طلب تأكيد البريد، افتح إيميلك واضغط الرابط).

> 🎉 الآن أي تعديل تسويه (مهمة، مشروع، فاتورة) يُحفظ:
> - **محلياً** (يشتغل أوفلاين)
> - **سحابياً** (يظهر على باقي أجهزتك خلال ثوانٍ)

### 2.5 تكرار تسجيل الدخول على الأجهزة الثانية

افتح التطبيق على أي جهاز ثاني → الإعدادات → نفس الـ URL والـ key → سجل دخول بنفس الإيميل → ستنزل كل بياناتك تلقائياً.

---

## 📱 المرحلة 3: تطبيق APK لأندرويد

### 3.1 تشغيل البناء

البناء يصير **تلقائي** عند كل push على branch `main`. لو تبي تشغله الحين:

1. ادخل على المستودع → تبويب **Actions**.
2. اختر workflow اسمه **Build Android APK** من اليسار.
3. اضغط **Run workflow** → **Run workflow** (الزر الأخضر).
4. انتظر ~ 5-7 دقائق.

### 3.2 تنزيل ملف APK

1. افتح آخر تشغيل ناجح (دائرة خضراء ✓).
2. تحت **Artifacts** ستجد `abd-saif-android-apk`.
3. اضغط لتحميله → استخرج الـ ZIP → داخله ملف `abd-saif-debug.apk`.

### 3.3 تثبيت APK على الموبايل

1. انقل الملف للموبايل (واتساب لنفسك / Google Drive / كابل USB).
2. افتحه في الموبايل.
3. أندرويد بيحذرك "App from unknown source" → اضغط **Settings** → **Allow from this source**.
4. اضغط **Install** → سيتنصب باسم **عبد سيف** مع الأيقونة.

> 💡 التطبيق سيشتغل بدون شريط متصفح، تماماً مثل أي تطبيق رسمي.

### 3.4 إصدار رسمي (Release بدلاً من Debug) — اختياري

عند إنشاء Tag بصيغة `v1.0.0`:
```bash
git tag v1.0.0
git push origin v1.0.0
```
سيُنشئ GitHub Release تلقائياً مع الـ APK مرفق.

> 🔒 لإصدار **Production** موقع رقمياً (للنشر على Google Play لاحقاً)، نحتاج keystore. هذه خطوة لاحقة، أخبرني وقت تحتاجها.

---

## 💻 المرحلة 4: تطبيق EXE لويندوز

### 4.1 تشغيل البناء

1. **Actions** → **Build Windows EXE (Tauri)** → **Run workflow**.
2. انتظر ~ 8-10 دقائق (Rust يأخذ وقت أول مرة، بعدها يستخدم cache).

### 4.2 تنزيل المثبّت (Installer)

1. افتح آخر تشغيل ناجح.
2. تحت **Artifacts** ستجد `abd-saif-windows`.
3. حمّل الـ ZIP → داخله ملفّان:
   - `*.msi` — مثبّت ويندوز قياسي
   - `*-setup.exe` — مثبّت NSIS بواجهة جميلة (موصى به)

### 4.3 التثبيت على ويندوز

1. شغّل أي ملف من الاثنين.
2. ويندوز يحذرك "Windows protected your PC" (لأن الملف مو موقع رقمياً)
   → اضغط **More info** → **Run anyway**.
3. اتبع المثبّت → سيظهر التطبيق في:
   - قائمة Start
   - سطح المكتب
4. افتح **عبد سيف** من قائمة Start.

> 💡 حجم التطبيق ~ 5 MB فقط (Tauri يستخدم WebView2 المدمج بويندوز).

> 🔒 لإلغاء تحذير Windows SmartScreen نهائياً، يحتاج Code Signing Certificate (~ $200/سنة). للاستخدام الشخصي، الـ "Run anyway" كافي.

---

## 🔄 المرحلة 5: تحديث التطبيق

### للويب
كل ما تسوي push للكود → GitHub Pages تتحدث تلقائياً (دقيقتين).

### للموبايل واللابتوب
1. اعمل push للتعديلات.
2. شغّل workflows مرة ثانية من تبويب **Actions**.
3. حمّل الـ APK / EXE الجديد ونصبهم فوق القديم.

### تحديث ذكي للويب فقط (بدون إعادة تنصيب APK/EXE)
لو التعديل بس على الكود (بدون ميزات أصلية)، **التطبيقات المنصبة سيشوفون التحديث تلقائياً** بسبب الـ Service Worker — اللي يحمل آخر كود من الويب.

---

## ❓ مشاكل شائعة

### "البناء فشل في GitHub Actions"
- افتح الـ workflow الفاشل واقرأ أول رسالة خطأ.
- معظم الأخطاء بسبب نسخة Java أو Node — كلها معرّفة بالفعل في الـ YAML.

### "ما تطلع لي بيانات بعد تسجيل الدخول"
- تأكد إنك نفّذت سكربت `supabase/schema.sql` كاملاً.
- في الإعدادات → الحساب → **رفع كل البيانات للسحابة** (المرة الأولى).
- ثم على الجهاز الثاني → **مزامنة الآن**.

### "Supabase يقول Email not confirmed"
- روح للـ Project → **Authentication** → **Providers** → **Email** → عطل **Confirm email** (للاستخدام الشخصي فقط).

### "APK ما ينصب على الموبايل"
- فعّل "Install from unknown sources" لتطبيق المتصفح/مدير الملفات اللي تستخدمه.
- تأكد إن الموبايل أندرويد 7+ (API 24+).

### "EXE يفتح ويسكر فوراً"
- ثبّت [Microsoft Edge WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/) (موجود تلقائياً في ويندوز 11 وأغلب أجهزة ويندوز 10 الحديثة).

---

## 📊 ملخص الأوامر السريعة

| تريد | اعمل |
|------|------|
| نشر تحديث ويب | `git push` (تلقائي) |
| بناء APK جديد | Actions → Build Android APK → Run |
| بناء EXE جديد | Actions → Build Windows EXE → Run |
| إصدار رسمي v1.x | `git tag v1.0.0 && git push origin v1.0.0` |
| تجربة محلياً | `python3 -m http.server 8080` |

---

## 🎯 الخلاصة

عندك الآن **منظومة كاملة**:
- ✅ موقع ويب على الإنترنت يتحدث تلقائياً.
- ✅ APK لأندرويد يُبنى بضغطة زر.
- ✅ EXE لويندوز يُبنى بضغطة زر.
- ✅ مزامنة سحابية تلقائية بين كل أجهزتك.
- ✅ يشتغل أوفلاين تماماً في كل الحالات.

أي مشكلة قابلتك، اسأل وأنا أساعدك! 🚀
